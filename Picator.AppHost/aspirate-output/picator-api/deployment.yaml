---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: picator-api
  labels:
    app: picator-api
spec:
  minReadySeconds: 60
  replicas: 1
  selector:
    matchLabels:
      app: picator-api
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: picator-api
    spec:
      containers:
      - name: picator-api
        image: picator-api:latest
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 8080
        - name: https
          containerPort: 8443
        envFrom:
        - configMapRef:
            name: picator-api-env
        - secretRef:
            name: picator-api-secrets
      terminationGracePeriodSeconds: 180